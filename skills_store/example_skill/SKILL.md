---
name: word_count
description: Counts words in a block of text. Example skill proving the SkillLoader discovery path (loader implementation lands in a future iteration).
---

# word_count

Counts the number of whitespace-separated words in the given text.

Invocation (once SkillLoader is implemented): `python run.py --text "..."`
